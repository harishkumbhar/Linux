---
- name: Audit /bin/bash users
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: Get all users with /bin/bash shell
      ansible.builtin.shell: |
        awk -F: '$7 == "/bin/bash" {print $1}' /etc/passwd
      register: bash_users
      changed_when: false

    - name: Create or clear local CSV file with headers
      ansible.builtin.copy:
        content: "IP Address,Username\n"
        dest: "./bash_users_report.csv"
        mode: '0644'
      delegate_to: localhost
      run_once: true

    - name: Append IP and Username to CSV file
      ansible.builtin.lineinfile:
        path: "./bash_users_report.csv"
        line: "{{ ansible_host | default(inventory_hostname) }},{{ item }}"
        create: no
      loop: "{{ bash_users.stdout_lines }}"
      delegate_to: localhost
